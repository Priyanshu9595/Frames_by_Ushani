kind = "web"
previewPath = "/"
title = "Frames by Ushani"
version = "1.0.0"
id = "artifacts/frames-by-ushani"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22353

[services.development]
run = "pnpm --filter @workspace/frames-by-ushani run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/frames-by-ushani", "run", "build" ]
publicDir = "artifacts/frames-by-ushani/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22353"
BASE_PATH = "/"
